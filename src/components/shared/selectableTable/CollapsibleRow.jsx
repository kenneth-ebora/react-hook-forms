/* eslint-disable react/prop-types */

import SelectableTableRow from "./SelectableTableRow";

const CollapsibleRow = ({ children }) => {
  return <SelectableTableRow>{children}</SelectableTableRow>;
};

export default CollapsibleRow;
